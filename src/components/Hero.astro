---

---

<section
  id="hero"
  class="relative min-h-screen flex items-center justify-center overflow-hidden"
>
  <!-- Particle background -->
  <canvas
    id="particle-canvas"
    class="absolute inset-0 w-full h-full pointer-events-none"
    aria-hidden="true"
  ></canvas>

  <!-- Ring halo -->
  <div class="absolute inset-0 flex items-center justify-center pointer-events-none">
    <div class="relative w-[600px] h-[600px]">
      <canvas
        id="ring-canvas"
        class="absolute inset-0 w-full h-full pointer-events-none"
        aria-hidden="true"
      ></canvas>
    </div>
  </div>

  <!-- Hero content -->
  <div class="relative z-10 text-center max-w-5xl mx-auto px-4">
    <p
      class="text-sm tracking-widest text-gray-500 uppercase mb-8"
      data-depth="0.3"
    >
      Medication Monitoring Platform
    </p>

    <h1
      class="text-6xl md:text-7xl lg:text-9xl font-light tracking-tight leading-[1.1] text-white mb-8"
      data-depth="0.5"
    >
      Smarter medication<br />
      monitoring starts here
    </h1>

    <p
      class="text-xl font-light text-gray-500 max-w-xl mx-auto mb-12 leading-relaxed"
      data-depth="0.7"
    >
      The first platform that gives Wisconsin pharmacies and providers
      real-time visibility into all prescription medications, not just
      controlled substances. Built for provider-to-provider coordination
      under HIPAA.
    </p>

    <div class="flex flex-wrap gap-6 justify-center" data-depth="0.9">
      <a
        href="#contact"
        class="cta-glow border border-white/20 hover:border-white/50 text-white text-sm tracking-widest uppercase px-10 py-4 hover:bg-white/5 transition-all duration-300"
      >
        Request Access <span class="ml-2">&rarr;</span>
      </a>
    </div>
  </div>
</section>

<style>
  .cta-glow {
    animation: borderPulse 3s ease-in-out infinite;
  }

  @keyframes borderPulse {
    0%, 100% {
      box-shadow: 0 0 0 0 rgba(6, 182, 212, 0);
      border-color: rgba(255, 255, 255, 0.2);
    }
    50% {
      box-shadow: 0 0 20px 0 rgba(6, 182, 212, 0.08);
      border-color: rgba(6, 182, 212, 0.3);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .cta-glow {
      animation: none;
    }
  }
</style>

<script>
  import { ParticleAnimation } from "../scripts/particles.js";
  import { RingHalo } from "../scripts/ringHalo.js";

  const particleCanvas = document.getElementById("particle-canvas");
  const ringCanvas = document.getElementById("ring-canvas");

  if (particleCanvas) {
    new ParticleAnimation(particleCanvas);
  }

  if (ringCanvas) {
    new RingHalo(ringCanvas);
  }
</script>
