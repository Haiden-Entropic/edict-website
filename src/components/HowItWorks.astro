---
const steps = [
  {
    number: "01",
    title: "Pharmacy Integration",
    description:
      "Your pharmacy management system already generates dispensing data in ASAP 4.2B format, the same standard used by Wisconsin's ePDMP. Edict connects to your existing workflow and begins ingesting prescription data for all non-OTC medications, not just controlled substances. No new hardware. No workflow disruption. Most pharmacies are live within 48 hours.",
  },
  {
    number: "02",
    title: "Continuous Monitoring",
    description:
      "Edict cross-references every prescription against a patient's complete medication profile in real time. Our platform flags dangerous drug interactions before dispensing, identifies adherence gaps across multiple providers, and detects duplicate therapy, covering the medications that state PDMPs miss entirely.",
  },
  {
    number: "03",
    title: "Provider-to-Provider Sharing",
    description:
      "When a risk is detected, Edict alerts the relevant prescribers and pharmacists through secure, HIPAA-compliant channels. Providers see a unified medication timeline, not fragmented records split across EHRs. No phone calls. No faxes. Coordinated care delivered automatically under HIPAA's Treatment, Payment, and Operations exception.",
  },
];
---

<section
  id="how-it-works"
  class="relative py-32 lg:py-48"
>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section header -->
    <div class="mb-20">
      <p class="text-sm tracking-widest text-gray-500 uppercase mb-6">
        How It Works
      </p>
      <h2 class="text-4xl md:text-5xl font-light tracking-tight max-w-2xl">
        Up and running in three simple steps
      </h2>
    </div>

    <!-- Steps -->
    <div class="border-t border-white/10">
      {steps.map((step) => (
        <div class="reveal border-b border-white/10 py-12 md:py-16 grid grid-cols-1 md:grid-cols-12 gap-6 md:gap-8 items-start">
          <div class="md:col-span-2">
            <span class="text-7xl font-extralight text-white/20 leading-none">{step.number}</span>
          </div>
          <div class="md:col-span-3">
            <h3 class="text-base font-medium tracking-wide text-white uppercase">{step.title}</h3>
          </div>
          <div class="md:col-span-7">
            <p class="text-gray-500 font-light leading-relaxed text-lg">{step.description}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
